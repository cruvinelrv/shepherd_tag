name: shepherd_tag
description: Lightweight tagging system for the Shepherd automation suite.
version: 0.0.2
homepage: https://github.com/cruvinelrv/shepherd_tag
repository: https://github.com/cruvinelrv/shepherd_tag
issue_tracker: https://github.com/cruvinelrv/shepherd_tag/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
